/* Deprecated deployment placeholder.
   All warehouse procedures, including document posting, are canonical in
   WarehouseModule.Procedures.sql. This file intentionally contains no DDL so
   running an old deployment step cannot override the canonical definitions. */
